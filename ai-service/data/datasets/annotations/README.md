# Annotation Exports (Task 138)

Store CVAT YOLO exports here:

```text
annotations/
└── exports/
    └── <batch_id>/
        ├── images/
        ├── labels/
        └── obj.names
```

Do not commit large export media to git. Keep manifests, QA logs, and protocol docs in repo.
