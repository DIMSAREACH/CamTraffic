{
  "name": "camtraffic-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test -c playwright.config.ts",
    "test:headed": "playwright test -c playwright.config.ts --headed",
    "test:officer-ai": "playwright test -c playwright.config.ts --project=user-chromium officer-ai-detection.spec.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.49.1"
  }
}
