# CamTraffic — Admin Portal

City administrator console. Runs on **http://localhost:5174**.

```bash
npm install
npm run dev
```

Structure: `admin/` (portal-specific), `shared/` (components, API, auth, common pages).
