# Train / val / test splits

Named split folders (e.g. `training_combined`, `*_dim_sareach`) are created by `ai/scripts/split_dataset.py` and related prep scripts. Media is gitignored; keep a `data.yaml` per split if you commit structure.
