# Production multi-class subset (`dataset_10`)

```
dataset_10/
├── data.yaml
├── classes.txt
├── images/train/
├── images/val/
├── labels/train/
└── labels/val/
```

Used by default YOLO train/eval scripts. Media is gitignored — add labeled pairs locally or regenerate via `scripts/build_dataset_10.py`.
