# CamTraffic — User Portal

Driver and traffic police app. Runs on **http://localhost:5173**.

```bash
npm install
npm run dev
```

Structure: `user/` (portal-specific), `shared/` (components, API, auth, common pages).
