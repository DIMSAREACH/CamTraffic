/**
 * Citizen settings — same shell as officer/driver shared UserSettingsPage.
 * Kept for backward-compatible imports from docs / older routes.
 */
export { UserSettingsPage as CitizenSettingsPage, UserSettingsPage as DriverSettingsPage } from '@user/pages/UserSettingsPage';
