# Production build — user portal (Traffic Ops + Citizen on app.*)
VITE_API_URL=https://api.camtraffic.store/api
VITE_USE_MOCK=false
VITE_USE_SAMPLE_FALLBACK=false
VITE_PORTAL_SURFACE=user
# Cross-portal redirects (required in production builds)
VITE_ADMIN_PORTAL_URL=https://admin.camtraffic.store
# Citizen deep links after Stripe/KHQR
# Success URLs should use /citizen/fines (see deploy/env/.env.production.example)
